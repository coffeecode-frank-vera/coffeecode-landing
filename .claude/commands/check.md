# /check — Run Astro type check

Runs `astro check` across all `.astro` and `.ts` files.

```bash
nvm use 24 && node_modules/.bin/astro check
```

Target: **0 errors, 0 warnings.** Hints are acceptable.
