# /dev — Start development server

Starts the Astro dev server on port 4321 using Node 24.

```bash
nvm use 24 && npm run dev
```

URL: http://localhost:4321

> Astro 6 requires Node ≥ 22. This project runs on Node 24 (LTS). `.nvmrc` is set to `24`.
